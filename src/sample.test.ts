import { describe, it, expect } from "vitest";

describe("Sample test", () => {
  it("works correctly", () => {
    expect(1 + 1).toBe(2);
  });
});

// https://huggingface.co/spaces/enzostvs/hub-api-playground/resolve/main/utils/datas/api_collections.ts
